# (1) 任意の変数に問題文をセットする
# ※「、」の1文字前が「で」になるような文章にしています
sentence = "文字列のスライスで、任意の変数に問題文をセットする"

# (2) 先頭から「、」の1文字前（「で」）までの文字列をスライス
# 「、」は9番目（インデックス 8）にあるので、0から8未満を指定します
sliced_text = sentence[0:8]

# (3) 上記(2)をprint文で表示する
print(sliced_text)
